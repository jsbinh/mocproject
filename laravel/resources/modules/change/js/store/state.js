// root state object.
// each Vuex instance is just a single state tree.
const state = {
    foo: 'bar',
    selectedNode: null,
    buttonNewChangeClicked: false,
    dataChanged: 0
}

export default state;
